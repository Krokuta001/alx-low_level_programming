#include "main.h"
#include <stdio.h>

/*a function that adds two integers and returns the result.
 * Return: Always 0.
 */

int main(void)
{
	n = add(80, 9);
        printf("%d\n", n);
        return (0);
}
